---
type: topic
status: active
created: 2026-08-09
updated: 2026-09-23
tags:
  - architecture
  - overview
---

# 系统架构

[[Wiki/MOC_项目知识|← 返回项目知识 MOC]]

## 技术栈与入口

- React 19 + TypeScript + Vite 7 + Tailwind CSS 4；Tailwind 通过 Vite 插件加载。
- dnd-kit 负责排序位移，framer-motion 精确锁定 `12.23.12`，`motion-dom` 同版本 override。
- Phosphor 与 Simple Icons 提供图标；Vitest + Testing Library 验证业务与组件，Playwright 验证桌面 Chrome 和 Pixel 7 模拟设备。
- `index.html` → `src/main.tsx` → `src/App.tsx` 为主应用；扩展另有 `popup.html` → `src/popup/main.tsx` 与 `src/background.ts` 两个入口。
- `vite.config.ts` 由构建 mode 决定网页 / 扩展入口、资源路径与输出目录；不维护两份应用源码。

## 模块边界

| 位置 | 职责 |
| --- | --- |
| `src/App.tsx` | 收藏与 GitHub 工作区、搜索、选择、弹窗、拖拽和设置草稿的界面编排；顶栏品牌使用无导航按钮 |
| `src/hooks/use-collection-selection.ts`、`src/lib/collection-selection.ts` | 收藏页面临时选择入口与纯状态变换，统一模式、集合、锚点及数据失效清理 |
| `src/components/grouped-collection.tsx`、`group-section-header.tsx`、`group-insert-control.tsx` | 分组组成、受控标题与菜单；sortable-group-section 只管理 dnd 外壳，局部样式在 grouped-collection.css |
| `src/components/collection-site-grid.tsx`、`compact-icon-button.tsx` | 平铺/单组/分组共用网站网格，加号/管理共用紧凑动作尺寸 |
| `src/hooks/use-site-dragging.ts`、`use-group-sorting.ts` | 网站事务与分组排序编排，内部使用原有业务函数/会话，不持有第二份持久化状态 |
| `src/hooks/use-drag-window-events.ts`、`use-tab-edge-scroll.ts`、`use-latest-event.ts`、`use-site-click-guard.ts` | 共用原生事件、取消、边缘滚动与最新回调；独立防误点击生命周期 |
| `src/lib/collection-drag-geometry.ts`、`collection-drag-ids.ts`、`collection-drag-sensors.ts` | DOM 测量、dnd 标识与激活距离，不反向依赖 UI |
| `src/hooks/use-group-sort-session.ts` | 分组排序的单次会话，统一活动块、轴、指针、冻结顺序和落点，finish 消费一次 |
| `src/hooks/use-image-import.ts` | 图片操作代次、处理中与错误状态，关闭、换源或卸载后禁止旧结果回写 |
| `src/components/appearance-settings.tsx`、`brand-settings-editor.tsx`、`src/lib/appearance-settings.ts` | 外观与品牌受控编辑视图、布局与文字方案纯变换；草稿仍由 settings-panel 持有 |
| `src/components/workspace-search.tsx` | 三工作区共用搜索外形、输入/清空和入场动画，业务提交与建议列表通过 props 注入 |
| `src/lib/collection-search.ts` | 收藏文本归一化、拼音/缩写和相关度；WorkspaceSearch 统一组合输入保护，历史搜索数据边界独立 |
| `src/hooks/use-group-manager-selection.ts` | 管理器批量圈选、全选/反选/范围与失效清理；编辑焦点和草稿独立 |
| `src/components/group-sort-preview.tsx` | 分组拖动内容预览及局部样式，复用 Favicon，不参与排序提交 |
| `src/components/card-primitives.tsx` | 收藏与历史共用卡片外壳、内容、选择按钮和导航/选择分流，dnd 位移仍由领域组件负责 |
| `src/components/history-cards.tsx`、`history-card-content.tsx`、`history-range-control.tsx` | 历史卡片适配、时间格式/图标和时间范围选择器；历史主视图保留选择、导航、滚动和拖拽编排 |
| `src/hooks/use-browser-history-data.ts` | 权限、订阅、查询防过期、删除去重与部分失败，不持有收藏或选择状态 |
| `src/components/wallpaper-settings-editor.tsx`、`data-settings-editor.tsx` | 壁纸受控编辑与独立数据操作，复用 RangeControl；不引入第二份设置草稿 |
| `src/components/resize-handle.tsx`、`topbar-resize-handle.tsx`、`src/hooks/use-settings-panel-width.ts` | 共用边界手势、键盘/取消、顶栏 Portal 和侧栏本地宽度偏好 |
| `src/components/dialog-navigation.tsx` | 编辑器共用的返回与关闭按钮，保留 Radix 焦点/关闭语义 |
| `src/lib/grouped-sites.ts` | 按组归类与组内顺序，供分组渲染和 Shift 选择共用 |
| `src/hooks/use-site-hub.ts` | 持久化收藏状态的 React 入口，编排加载、订阅、保存和业务操作 |
| `src/hooks/use-theme.ts`、`use-wallpaper.ts` | 主题解析与系统偏好订阅、壁纸资源生命周期；颜色共用 src/theme.css |
| `src/lib/site-state.ts` | 添加、编辑、分组、回收站和仓库导入等状态变换 |
| `src/lib/state-store.ts` | 网页 localStorage / 扩展 chrome.storage.local 适配与扩展变更订阅 |
| `src/lib/storage.ts` | `site-hub:v1` 数据解析、默认值补全及逐级迁移至 state v16 |
| `src/lib/data-transfer.ts` | 整库与分组资源包的序列化、校验与导出边界 |
| `src/lib/github-workspace.ts`、`github-repository-api.ts` | GitHub URL 路由、工作区兼容与公开仓库分页读取 |
| `src/lib/browser-runtime.ts`、`browser-history.ts`、`bookmark-manager.ts` | 浏览器 API 能力检测、权限与平台适配 |
| `src/components/` | 收藏卡片、分组、设置、GitHub 导入 / 刷新详情及独立历史视图 |
| `src/popup/` | 当前页收藏与浏览器书签管理，复用存储与业务模块 |
| `src/background.ts` | MV3 service worker 历史变更事件通知桥 |
| `src/data/`、`src/types.ts` | 默认数据、图标选项与共享类型 |
| `src/theme.css`、`src/styles.css` | 共用浅深颜色令牌、全局样式、组件样式与响应式规则 |

E2E 按收藏、分组管理、分组排序、网站拖拽、GitHub、历史、设置、布局图标和 popup 分域，共用 `e2e/fixtures.ts` 初始化、API 隔离与页面异常检查。

模块行数与维护优先级只在带日期的 [[Wiki/工程维护清单|工程维护清单]] 中记录，避免作为长期接口事实反复复制。

## 状态与数据流

界面操作经 `useSiteHub` 调用业务函数得到下一份收藏 state；同批命令读取同步最新快照，再交给存储适配器保存，失败显示持久错误并可重试；扩展 popup 使用同一存储契约，存储变更订阅将其他扩展页面的修改反馈到主应用。

持久化收藏 state 与页面临时状态分开：拖拽预览、弹窗、搜索上下文和未保存设置草稿由界面维护，确认后才提交。历史视图直接读取浏览器历史 API，不写入收藏 state；本地壁纸二进制保存在 IndexedDB。

收藏选择只保留一份带类型的集合与锚点；App 发出切换、组内全选、拖拽开始、取消等动作。选择不写持久化 state；真实数据更新可修剪失效 ID，临时拖拽预览不得作为修剪依据。分组排序会话由 useGroupSortSession 管理，useGroupSorting 负责输入/测量连接，group-sort 提供纯落点计算；网站事务由 useSiteDragging 编排。两者共用取消与标签滚动，DOM 测量独立为 collection-drag-geometry，完整维护入口见 [[Wiki/收藏分组模块边界|收藏分组模块边界]]。

## 保持的约束

- 保留自研 hook 与业务函数的边界，不引入 Redux / Zustand 作为第二份状态源（[[Wiki/决策/ADR-001|ADR-001]]）。
- state 新增字段必须迁移且只补不覆盖；存储键不随产品文案或仓库名称变化（[[Wiki/数据与存储契约|数据与存储契约]]）。
- 排序位移只由 dnd-kit 控制，不叠加 Motion transform；拖拽修改先读 [[Wiki/拖拽交互契约|拖拽交互契约]]。
- `npm run dev` / `runStart.cmd` 默认使用 5173；E2E 与截图使用 4173。
- `dist/`、`dist-extension/`、测试报告和本地验收产物是生成物；源码中仅保留 `docs/images/` 的少量公开展示图片。

## 维护方向

2026-09-19 专项已将收藏选择收敛为独立 reducer/hook，平铺与分组共用卡片配置；现有持久化分层继续使用。分组排序会话、E2E 分域和外观编辑边界已提取；后续优先处理网站拖拽编排，设置的壁纸/数据视图和历史数据读取已于 1.1.80 提取，卡片、搜索、数值控件与边界拖动均有实际共享入口；1.1.81 已提取网站排序几何、原生事件、标签滚动及分组展示；后续按实际失败证据处理网站意图复杂分支与壁纸定位。详见 [[Wiki/工程维护清单|工程维护清单]]。

## 依据

- [[Wiki/日志/工程维护/第011次|工程维护·第011次]] — 本期源码核对、结构评估与验证记录。

## 相关页面

- [[Wiki/MOC_项目知识|项目知识 MOC]]
- [[Wiki/数据与存储契约|数据与存储契约]]
- [[Wiki/拖拽交互契约|拖拽交互契约]]
- [[Wiki/扩展与构建交付|扩展与构建交付]]
- [[Wiki/工程维护清单|工程维护清单]]
