---
type: topic
status: active
created: 2026-08-09
updated: 2026-09-25
tags:
  - storage
  - data-model
---

# 数据与存储契约

[[Wiki/MOC_项目知识|← 返回项目知识 MOC]]

> 收藏持久化经 `src/lib/state-store.ts` 适配网页/扩展，解析与迁移由 `src/lib/storage.ts` 负责；本地图片经 wallpaper-store 保存。任何新增字段或结构变更必须遵循下方铁律。

## 存储键与版本模型

- 存储键**永远为 `site-hub:v1`**，不随版本更换（见 [[Wiki/决策/ADR-003|ADR-003]]）。
- 数据版本号写入 state 内部字段（非键名）。当前版本已演进至 v17。
- 读取时按 `storage.ts` 逐级从 v1 迁移到当前版本。

## 迁移铁律

1. **只补默认字段，绝不覆盖用户已有收藏**。
2. 解析失败时不覆盖损坏的源值，仅回退到安全默认值并记录。
3. 新增 state 字段必须写迁移分支，默认值是安全占位。
4. 迁移幂等：重复迁移不产生副作用。

## clamp 容错与 recovered 语义

- 数值类字段（字号、间距、比例等）读取后做 `clamp` 到合法区间，避免越界导致渲染崩溃。
- 核心收藏结构无效时返回安全默认值并标记 `recovered`，损坏源值保持不动；这不代表自动恢复了其中每一条收藏。外观等可安全补全的字段使用规范化函数，保留有效值。
- 活动网站 ID 必须唯一且非空，名称非空、URL 为 HTTP(S)，顺序为有限数值，创建/修改时间可解析；非法核心字段不能作为正常快照写回。
- 即使输入已标记 v17，读取出口仍需规范化工作区显示/排列、点击数、回收站和外观等偏好；缺失偏好不能令正常收藏进入误恢复或返回缺字段状态。有效值保留，重复读取幂等；可选的 githubMigration 缺失仍保持缺失，以遵守便携导出边界。

## SiteItem 双序号

- 每个站点同时持有**组内序号**与**全局序号**两个排序字段（见 [[Wiki/日志/功能添加/第004次|功能添加·第004次]]）。
- 组内视图按组内序号排序；全局/跨组拖拽按全局序号重排，落库时同步回填组内序号。
- 名称/时间排序下的仅迁移拖拽不重排 `globalOrder`；站点移入目标组后追加到该组手动序末尾，再由当前临时排序规则决定显示位置。
- 每个站点持有持久化的 `clickCount`；真正打开网站时递增，热量排列按次数降序展示，并列时回到当前手动顺序，不因点击改写 `updatedAt`。

## SiteGroup.isProtected

- 分组有 `isProtected` 标志；受保护分组（如"其他"）不被用户删除，仅在加载时对重复"其他"做无损合并。
- 删除普通分组不再选择处理策略：空组直接删除，非空组把站点以完整快照写入 `deletedSites` 后删除分组；受保护分组仍不可删除。
- 分组排序和指定位置创建继续复用 `SiteGroup.order`：`addGroup(name, icon, beforeGroupId?)` 可插入目标分组之前，写入后统一归一化顺序；受保护的“其他”固定末尾，不新增 state 字段或迁移。

## 管理器批量删除（1.1.82）

- `deleteGroupsFromState` 在一次不可变状态变换中去重 ID、排除受保护分组、追加网站回收站快照并归一化剩余顺序；单组删除调用同一函数。不循环多次写库，不丢弃既有回收站数据。
- 每个回收项保留自己的原组名/工作区和同批时间。未知、已删除或受保护 ID 无操作；输入 state 不被修改。确认与临时选择属于管理器，不新增 state 字段；1.1.82 当时为 v15 / site-hub:v1。
- 收藏搜索的派生文本/拼音索引仅内存缓存；相关度不回写手动顺序，详见 [[Wiki/收藏搜索匹配|收藏搜索匹配]]。

## 批量跨组迁移

- 批量迁移一次性接收选中网站 ID、目标分组与当前稳定可见顺序；已在目标组的网站保持原位，其余选中项按稳定顺序追加到目标组手动序末尾。
- 迁移只更新 `groupId`、受影响组的 `order` 与 `updatedAt`，保留每个网站的 `globalOrder`；同组或没有实际迁移项时返回原快照。

## v9 链接回收站

- v9 新增 `deletedSites: TrashedSite[]` 与 `trashRetentionDays: 7 | 30 | 90 | null`；v8→v9 迁移只补空回收站与默认 30 天，不覆盖已有收藏。
- `TrashedSite` 保存完整 `SiteItem`、删除时间、原分组 ID 和原分组名称。恢复时原分组存在则追加到其手动序末尾；原分组已删除则恢复到受保护的“其他”。
- 应用加载、页面重新可见、期限变更及最近过期时间到达时执行自动清理；`null` 表示永不自动清理。自动过期属于确定性维护，不需要二次确认。
- 网站删除与分组删除都写入回收站；逐项永久删除和清空回收站使用 2 秒内二次点击确认。

## v10 点击热量

- v9→v10 迁移为现有活动站点与回收站快照补 `clickCount: 0`；已有合法计数保持不变，非法或缺失计数归零，不覆盖收藏内容。
- 新增网站与分组资源包导入的网站从 0 次开始；整库导出保留点击计数，分组资源包不携带本机热量数据，导入后重新从 0 次统计。

## v11 GitHub 工作区与 v12 独立显示方式

- `SiteGroup.workspace` 取 `main` 或 `github`；旧版分组在 v10→v11 迁移时补为 `main`，并补齐 GitHub 的“仓库、工具、文档、其他”四个分组。GitHub “其他”受保护且固定在 GitHub 工作区末尾。四个默认组仅在工作区首次创建时补齐；之后允许删除全部普通组，加载和再次进入不得重建它们，v11–v16 校验只要求 GitHub 有且仅有一个受保护的“其他”。
- GitHub URL 通过 URL 解析精确匹配 `github.com` 或 `*.github.com`，不匹配 `github.io`、相似域名或无效 URL；精确根路径 `github.com` / `www.github.com` 由 `isGithubHomeUrl` 单独识别为官方共享入口。主收藏中的具体 GitHub 链接自动路由到 GitHub “其他”；GitHub 工作区新增、编辑、拖入和导入拒绝非 GitHub URL，也拒绝直接把官方根入口写入普通 GitHub 分组。
- `https://github.com/` 只有一条 `SiteItem`，归属主收藏并在 GitHub 页顶部渲染共享入口；编辑、删除会同步两个显示位置，固定入口整卡可打开 GitHub，右侧刷新按钮打开作者仓库导入/刷新流程，入口菜单只保留编辑与删除；旧数据若仍在 GitHub 分组不会被首次迁移自动改动。
- 首次进入 GitHub 工作区时只移动现有具体 GitHub 站点，不复制数据；`githubMigration` 仍保存原分组、组内/全局排序、原名称和 URL，保留兼容读取与底层恢复能力，但当前 UI 不再提供迁移横幅、放回主页或一次性撤销入口。原分组不存在时回退主收藏“其他”。
- 完整导入后统一执行 GitHub 路由；GitHub 站点与 GitHub 分组随整库导出，`githubMigration` 仅为本地撤销记录，不进入导出文件。
- v11→v12 新增 `displayModeByWorkspace: { main: "grouped" | "flat", github: "grouped" | "flat" }`；旧 `displayMode` 只补全 `main`，GitHub 默认 `flat`，已有 v12 工作区选择不会被迁移覆盖。旧 `displayMode` 字段继续保留兼容读取。

## v13 跨工作区搜索与 GitHub 作者仓库导入

- v12→v13 为 GitHub 分组增加可选 `githubImportSource`：`{ login, profileUrl, entityType, lastFetchedAt? }`。迁移只补缺失字段并保留已有收藏、分组和显示方式，重复读取幂等；存储键仍为 `site-hub:v1`。
- 收藏主页、GitHub 页面和任意具体分组在搜索词非空时都读取活动收藏的完整主收藏与 GitHub 工作区，统一平铺展示并带工作区/分组标识；从具体分组开始搜索时暂时进入全库搜索，清空关键词后恢复原工作区和分组。搜索字段包括名称、主机名、URL 和分组名，搜索期间禁止拖拽排序；回收站不参与收藏搜索。
- 浏览器历史视图拥有独立的搜索边界，只搜索浏览器历史，不混入收藏链接，也不受收藏页面的全局搜索状态影响。
- 作者导入只接受 `https://github.com/<登录名>` 或 `www.github.com` 的单段作者/组织主页，不接受仓库、Issue、`github.io`、Enterprise 或相似域名。网页端直接使用 GitHub 公开 REST API；扩展端声明最小 `https://api.github.com/*` 主机权限，不保存 Token。
- 作者/组织识别与仓库列表采用串行分页请求，每页最多 100 条；所有分页成功后才允许写入。导入先生成预览，确认后按登录名创建或复用 GitHub 分组；同名分组若绑定其他来源则加后缀。活动收藏和回收站全局去重，不覆盖用户修改，也不创建空分组。
- 作者分组保存来源信息用于手动刷新；顶部官方入口会按登录名去重并一次读取全部已导入作者来源，完整读取后只追加不存在的仓库，不删除 GitHub 已消失的旧收藏，也不重新加入回收站中的 URL。单个来源失败不回滚其他已成功来源，刷新详情按作者显示本次新增仓库、重复跳过和失败原因，但该报告是临时 UI 状态，不写入 `site-hub:v1`；来源信息随整库 JSON 导出，分组资源包不携带来源关系。
- 未登录公开 API 受 GitHub 速率限制，遇到 403/429 或任意分页失败时只显示可重试错误，不创建、不修改、不部分导入。

## v14 持久化排列方式

- v13→v14 新增 `sortModeByWorkspace: { main, github }`，分别保存收藏主页与 GitHub 工作区的排列方式；旧 `sortMode` 字段只作为主工作区兼容回退并继续保留。
- 排列方式取 `manual`、`name-asc`、`name-desc`、`newest`、`oldest` 或 `heat`；迁移只补缺失或非法值为 `manual`，保留已有合法选择，重复读取幂等。
- 选择“热量排列”时，卡片在网址下方、组名上方显示该站点的纯数字点击次数，数据来自持久化 `SiteItem.clickCount`；不显示火焰图标、“热量”或“次”，也不改变手动序号。

## v17 壁纸玻璃偏好

- 1.1.87 在 wallpaper 补充 topbarStyle、glassTransparency、glassBlur、glassSaturation、glassHighlight、glassRefraction、glassRefractionStrength。默认分别为 clear、78、12、130、45、false、24；范围及 UI 语义见 [[Wiki/外观与设置体系|外观与设置体系]]。
- upgradeToVersion17 只补缺失参数，保留有效值、旧图片来源/构图、topbarOpacity/Blur、收藏与分组；顶栏透明度 UI 做 100 减 topbarOpacity，不翻转持久化字段语义。缺失/非法值回退，有限数值限幅，布尔值不接受字符串真值。
- 当前版本读取、旧版迁移、设置保存、整库导入均经 normalizeWallpaper；重复读取幂等。折射贴图和浏览器能力不持久化，无新增存储键或图片副本。

## v16 主题偏好

- 新增 `appearance.theme: system | light | dark`，属于统一设置草稿、保存与整库 JSON 备份；导出外壳版本不变，不增加独立 localStorage 主题键。
- 新安装默认 system。v15 及更早快照缺失/非法时只补 light，保留升级前固定浅色；已有合法主题继续保留。源字段在进入旧版升级链之前取证，防止中间默认值把老用户误改为 system。
- v16 的缺失/非法主题回落 system；重复解析幂等。原分组、网站、几何、字号、品牌与壁纸不因主题迁移被覆盖。
- 已废弃的根字段 themePreference 不恢复；v14→v15 原分支仍负责丢弃它。

## v15 固定浅色外观（历史迁移）

- v14→v15 移除 `themePreference` 状态字段；当时页面固定使用浅色；该历史迁移继续保留，当前主题由 v16 的 appearance.theme 承担。
- 迁移只丢弃已废弃的主题字段，其他 state 字段、收藏、分组和用户外观参数均保留；重复读取幂等。

## 外观界面简化的兼容边界

- 1.1.79 移除五分类数值编辑器，但不删除 AppearanceSettings 字段、不变更 state v15。旧自定义几何与字号继续读取和导出；改主题色、名称或直接保存不清除隐藏参数。
- 新布局方案只替换几何，字号方案显式统一全局与局部文字比例；完整恢复默认外观才恢复全部外观字段。界面详情见 [[Wiki/外观与设置体系|外观与设置体系]]。

## 1.1.80 尺寸与恢复兼容

- 卡片高度和顶栏高度使用既有 appearance 字段，侧栏宽度继续使用既有独立 localStorage 偏好。state 仍为 v15，site-hub:v1、导出格式和迁移分支不变。
- 最近布局快照仅属于本次设置会话，跨页签保留，关闭后不另行落库；恢复仅复制布局几何，保留后续修改的颜色与字号。保存/取消仍作用于统一草稿。
- 浏览器历史的查询、权限、事件订阅与删除收敛到 useBrowserHistoryData；旧查询返回不得覆盖新查询，重复删除期间拒绝重复提交，部分失败只返回成功 URL 给选择层清理。

## 导出格式

- 导出/导入为自描述 JSON，包含 state 版本号；导入时按目标版本号走同一迁移路径，保证前向兼容。导出明确清空 `deletedSites`，不携带本机回收站；导入与恢复保留本机既有回收站和期限设置。

## 分组资源包

- 分组资源包使用独立的 `site-hub-group-export` 格式和版本号，不会被整库导入流程误识别。
- 资源包只包含来源分组的名称、图标和按组内手动顺序排列的网站便携字段（名称、规范化 URL、可选自定义图标 URL、图标来源）；不包含本机 ID、分组 ID、`globalOrder`、回收站、搜索历史、壁纸或其他设置。
- 从“管理分组”导出当前选中组；导入时追加到当前选中的目标组，保留目标组名称和图标。
- 导入按规范化 URL 做全局去重：现有收藏和资源包内部重复项均跳过，新网站以新 ID 追加到目标组手动序末尾；目标组为受保护的“其他”时仍只追加网站，不修改保护属性。
- 资源包字段验证失败时拒绝整个导入，不静默丢弃无效网站；取消预览确认不写入 state。

## 跨上下文同步

- 网页版用 `localStorage`；扩展版用 `chrome.storage.local`，经存储适配器在运行时切换（见 [[Wiki/扩展与构建交付|扩展与构建交付]]）。
- 扩展多实例（popup / 新标签页）共享 `chrome.storage.local`，写后广播变更以刷新各视图。
- useSiteHub 的所有命令使用同步更新的最新快照；外部同步只跳过该份快照的保存，同批次后续本地编辑仍需落库。直接保存搜索与 effect 保存同一快照时复用同一 Promise；失败保留错误提示和重试入口。
- popup 必须保存成功后才更新界面，写入前重新读取最新收藏；读取失败显示“重新读取”，标签页/书签等可选能力失败不阻断其他入口。写前读取不能提供跨上下文原子事务，真正同时写入仍需专项设计。

## 浏览器历史边界

- 浏览器历史不是 Mysimple state 的字段：历史视图通过 `chrome.history` 查询当前 Chrome / Edge 配置文件，不读取或写入 `site-hub:v1`，也不进入整库或分组资源包导出。
- 历史权限采用 manifest 的可选 `history` 权限，在用户首次进入历史视图时请求；拒绝权限只影响历史视图，不覆盖收藏数据。
- 批量删除只从选择集合移除已成功的 URL；失败项保留选中，可继续重试。
- 历史列表先按 URL 去重，再在界面按主域名聚合为站点卡片；卡片内部仍以 URL 为最小展示与选择单位。单条、批量和范围清理直接调用浏览器 API，浏览器原生删除则通过历史事件通知与重新查询同步到 Mysimple。

## 依据

- [[Wiki/日志/功能添加/第001次|功能添加·第001次]]、[[Wiki/日志/功能添加/第002次|功能添加·第002次]]、[[Wiki/日志/功能添加/第003次|功能添加·第003次]]、[[Wiki/日志/功能添加/第004次|功能添加·第004次]]、[[Wiki/日志/功能添加/第006次|功能添加·第006次]]、[[Wiki/日志/功能添加/第009次|功能添加·第009次]]、[[Wiki/日志/功能添加/第011次|功能添加·第011次]]、[[Wiki/日志/功能添加/第012次|功能添加·第012次]]、[[Wiki/日志/功能添加/第016次|功能添加·第016次]]、[[Wiki/日志/功能添加/第017次|功能添加·第017次]]、[[Wiki/日志/功能添加/第019次|功能添加·第019次]]

## 相关页面

- [[Wiki/MOC_项目知识|项目知识 MOC]]
- [[Wiki/系统架构|系统架构]]
- [[Wiki/扩展与构建交付|扩展与构建交付]]
