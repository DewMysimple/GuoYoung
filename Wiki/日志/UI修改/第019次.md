# 第019次

- 时间：2026-08-24 12:48:10—12:51:30（北京时间）
- 对话类型：UI修改
- 主题标签：分组多选、分组拖动、虚线外框、重复边框、1.1.35
- 源自原轮次：无
- 本轮目标：移除多选小组状态中与整组虚线外框重复的紧凑实线内框。
- 日志索引：[[Wiki/日志/MOC_工作日志|工作日志 MOC]]
- 分类索引：[[Wiki/日志/UI修改/MOC_UI修改|UI修改 MOC]]
- 相关知识：[[Wiki/当前状态|当前状态]]、[[Wiki/拖拽交互契约|拖拽交互契约]]、[[Wiki/扩展与构建交付|扩展与构建交付]]、[[Wiki/测试与验证基线|测试与验证基线]]

## 已确认的决策

- 选择小组和拖动小组继续共用整组 `.grouped-site-section` 状态层，只保留外层蓝色虚线与连续蓝色底，不绘制紧凑标题区域的实线内框或 inset 描边。
- 不改变拖拽命中、实时让位、松手提交、多选状态或持久化结构；扩展版本递增至 1.1.35。

## 检查与操作

- 核对选中分组样式，确认重复线来自 `.grouped-site-section.is-group-selected` 的 inset box-shadow。
- 移除 inset 描边，保留与 outline offset 对齐的 spread shadow，并新增 computed box-shadow 回归断言。
- 通过分组多选截图检查整组虚线框内只剩连续蓝色底，没有紧凑实线内框。

## 文件变更

- `src/styles.css`：移除选中分组状态的 inset 实线内框。
- `e2e/site-hub.spec.ts`：增加 box-shadow 不含 inset 的回归断言。
- `public/manifest.json`：版本递增至 1.1.35。
- `Wiki/`：更新拖拽契约、当前状态、构建交付、验证基线与双重 MOC 登记。
- `dist-extension/`、`artifacts/site-hub-extension/`、`artifacts/site-hub-extension.zip`：重新生成 1.1.35 交付物。

## 测试与验证

- `npm.cmd test -- --run`：15 个测试文件、122/122 通过。
- 定向 E2E：3 passed / 1 skipped，覆盖分组多选与分组拖动源；截图写入 `artifacts/releases/v1.1.35/screenshots/` 并人工检查。
- `npm.cmd run test:e2e`：106 次执行，77 passed / 29 skipped。
- `npm.cmd run build`：通过，类型检查与网页生产构建完成。
- `npm.cmd run package:extension`：通过；1.1.35 ZIP 为 `artifacts/site-hub-extension.zip`，324884 bytes，SHA-256 `DACFA7A2DA066001CE06C48E7B2686D93D28A6522BC0B01FBDCE19695A8D714D`。
- `npm.cmd run lint:wiki`：通过；Wiki 校验为知识页 31 个、日志 70 篇、链接 945 条；已人工检查 1.1.35 小组多选截图。

## 问题、结果与下一步

- 重复的紧凑实线内框已移除，选中和拖动状态只保留整组虚线外框及连续蓝色底；未扩大任何交互命中区域。
- 当前工作区会提交并推送本轮修改；安装 1.1.35 后请到扩展管理页点击“重新加载”。
