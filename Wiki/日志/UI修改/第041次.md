# 第041次

- 时间：2026-09-26 14:39:42—15:05:57（北京时间）
- 对话类型：UI修改
- 主题标签：收藏卡拖拽、落点透明度、来源虚影、壁纸玻璃、1.1.94
- 源自原轮次：无
- 本轮目标：修正“拖到另一张卡片上时目标卡整体透明度没有变化”，并将小红书来源原位虚影不透明度提高 10 个百分点。
- 日志索引：[[Wiki/日志/MOC_工作日志|工作日志 MOC]]
- 分类索引：[[Wiki/日志/UI修改/MOC_UI修改|UI修改 MOC]]
- 相关知识：[[Wiki/拖拽交互契约|拖拽交互契约]]；[[Wiki/外观与设置体系|外观与设置体系]]；[[Wiki/测试与验证基线|测试与验证基线]]

## 已确认的决策

- 真实命中的收藏卡片整卡 opacity 设为 0.78；被拖动收藏卡片留在原位的虚影由 0.16 提高到 0.26。浏览历史卡拖动样式仍为 0.16。
- 壁纸下目标卡玻璃底色 alpha 大于 0.75，模糊与折射继续启用。底色 alpha 和整卡 opacity 是两层样式，必须分别验证。

## 检查与操作

- 回读拖拽契约、外观与设置、测试基线，并核对 SiteCard 的 `is-drop-target` 状态和两份拖拽 CSS。
- 确认此前实现只加深玻璃 `background-color`，目标元素计算 opacity 仍为 1，因此视觉上的整卡透明度没有改变。
- 首次全量 E2E 暴露共享 `.site-card.is-dragging` 也命中浏览历史卡；将提高来源虚影的规则收窄到带 `data-site-dnd-id` 的收藏卡，再定向及全量复验。
- 扩展版本递增至 1.1.94；在生产玻璃截图脚本加入小红书拖至哔哩哔哩的真实拖动与样式断言。

## 文件变更

- 修改：`src/styles.css`、`e2e/site-drag.spec.ts`、`e2e/glass.spec.ts`、`scripts/capture-glass.mjs`、`public/manifest.json`、拖拽契约、外观与设置、当前状态、测试与验证基线及双 MOC。
- 新增：本篇日志。
- 生产截图：`artifacts/releases/v1.1.94/screenshots/glass-production-xiaohongshu-over-bilibili.png`。

## 测试与验证

- `npm.cmd run typecheck` 通过；`npm.cmd test`：45 个文件、335 项通过。
- 第一次完整 E2E 出现 2 项失败，均为历史卡虚影期望 0.16、实际收到 0.26；根因是共享 `.site-card.is-dragging` 选择器。收窄至收藏卡后，定向回归 5 passed / 1 skipped；再次完整运行 `165 passed / 43 skipped / 0 failed`。
- 网页生产构建、扩展生产构建和 `npm.cmd run package:extension` 通过；保留既有大 chunk 提示。标准 `npm.cmd run capture` 六个状态无横向溢出、无页面异常；控制台记录一条 `NotSameOrigin` 资源拦截信息，来源未定位。专项生产玻璃捕获生成 14 张截图，页面异常为 0；真实落点 opacity 0.78、来源 opacity 0.26、玻璃填色 alpha 0.85896，`backdrop-filter` 含 2px blur 和透镜折射。
- `dist-extension`、固定解压目录和 ZIP 内容均为 23 个文件且逐文件 SHA-256 一致；ZIP SHA-256：`255621D96E8B2548A876ACAA759EEC46D790AF00F670838CD8DDF4C39E9A018B`。
- `npm.cmd run lint:wiki` 通过（知识页 39 个、日志 135 篇、链接 1716 条）；`git diff --check` 通过。

## 问题、结果与下一步

- 根因是上次只提高壁纸玻璃底色 alpha，没有设置目标卡元素自身的 opacity；来源虚影调整误用共享卡片选择器也会影响浏览历史，已分别修正。
- 1.1.94 的计算样式、生产拖动截图和全量 E2E 证实目标卡 opacity 变化且模糊/折射仍在，历史卡拖动保持旧值。代码推送后，请到扩展管理页点击“重新加载”。
