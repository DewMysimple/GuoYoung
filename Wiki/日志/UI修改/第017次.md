# 第017次

- 时间：2026-08-24 12:16:09—12:21:56（北京时间）
- 对话类型：UI修改
- 主题标签：分组多选、分组拖动、圆角、视觉反馈、1.1.33
- 源自原轮次：无
- 本轮目标：收紧多选小组与拖动小组的外框圆角，使两种状态的边界更利落并保持整组反馈一致。
- 日志索引：[[Wiki/日志/MOC_工作日志|工作日志 MOC]]
- 分类索引：[[Wiki/日志/UI修改/MOC_UI修改|UI修改 MOC]]
- 相关知识：[[Wiki/当前状态|当前状态]]、[[Wiki/拖拽交互契约|拖拽交互契约]]、[[Wiki/扩展与构建交付|扩展与构建交付]]、[[Wiki/测试与验证基线|测试与验证基线]]

## 已确认的决策

- 多选小组与拖动小组共用紧凑的 `calc(var(--radius-card) + 2px)` 外框圆角；保留蓝色虚线、整组浅蓝背景及拖动源可读性。
- 只调整分组状态外框，不改变网站卡片、标题按钮、拖拽命中区域或持久化结构。
- 扩展版本递增至 1.1.33，固定交付路径继续使用 `artifacts/site-hub-extension/` 与 `artifacts/site-hub-extension.zip`。

## 检查与操作

- 将 `.grouped-site-section.is-group-sorting` 与 `.grouped-site-section.is-group-selected` 的圆角从 `+5px` 收紧为 `+2px`。
- 为纵向分组拖动和小组多选 E2E 增加 18px 计算圆角断言。
- 更新扩展 manifest、构建交付物、当前状态、拖拽契约、测试基线与版本知识页。

## 文件变更

- `src/styles.css`：收紧两种分组状态的外框圆角。
- `e2e/site-hub.spec.ts`：增加拖动和多选状态圆角断言。
- `public/manifest.json`、`dist-extension/manifest.json`、`artifacts/site-hub-extension/manifest.json`、`artifacts/site-hub-extension.zip`：递增至 1.1.33。
- `Wiki/`：更新版本说明、验证基线、拖拽契约和双重 MOC 登记。

## 测试与验证

- `npm.cmd test -- --run`：15 个测试文件、122/122 通过。
- 定向圆角 E2E：chromium 3 passed、mobile 1 skipped；完整 `npm.cmd run test:e2e`：76 passed / 28 skipped。
- `npm.cmd run build`、`npm.cmd run package:extension`：通过；ZIP 为 `artifacts/site-hub-extension.zip`，324664 bytes，SHA-256 `DE9465DC1087572069710060B601271515AA4DF572200F852A6EA98543C44CA6`。
- `npm.cmd run lint:wiki`：通过；Wiki 校验为知识页 31 个、日志 68 篇、链接 921 条；已检查 1.1.33 分组多选与拖动截图。

## 问题、结果与下一步

- 目标是让两种分组状态的外框边界更干净，同时保留实时让位和整组选中反馈。
- 安装 1.1.33 后请到扩展管理页点击“重新加载”。
