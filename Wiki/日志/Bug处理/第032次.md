# 第032次

- 时间：2026-09-25 19:37:22（北京时间）
- 对话类型：Bug处理
- 主题标签：玻璃分层、阴影、历史刷新、公共样式、页脚、1.1.89
- 源自原轮次：无
- 本轮目标：修复分组与公共区域材质遗漏、过重阴影、卡片工具外形、页脚底板以及历史记录空态闪动，收敛代码维护边界。
- 日志索引：[[Wiki/日志/MOC_工作日志|工作日志 MOC]]
- 分类索引：[[Wiki/日志/Bug处理/MOC_Bug处理|Bug处理 MOC]]
- 相关知识：[[Wiki/外观与设置体系|外观与设置体系]]、[[Wiki/数据与存储契约|数据与存储契约]]、[[Wiki/工程维护清单|工程维护清单]]、[[Wiki/测试与验证基线|测试与验证基线]]

## 已确认的决策

- 普通按钮、卡片、菜单和面板分别控制透明度，公开阴影强度，默认减轻且 0 可关闭投影；复用原壁纸设置草稿、预览/保存/取消和重置入口。
- 分类标签不使用外投影，卡片操作沿用卡片底色、悬停时轻量反馈；设置侧栏无外阴影，页脚使用无底板与阴影的普通文字。
- 历史结果仅保留在当前页面内存，跨工作区不卸载数据容器；交互内容仍按可见性挂载，隐藏停止查询，权限撤销立即清空。
- state v18 迁移只补不覆盖；存储键、Motion 版本、拖拽位移与既有交互保持。

## 检查与操作

- 对照用户截图回读分组、材质、卡片、Portal 面板及历史状态路径，清除局部旧玻璃覆盖和主题中被覆盖的默认值，减少重复填色与投影。
- 历史首次读取不显示空结果；仅输入搜索防抖，工作区重新进入保留上次结果；读取错误与删除错误分开，保留部分删除失败提示。
- 桌面生产截图覆盖网页和独立原生扩展，真实 Chromium 权限与历史验收使用全新配置和合成记录；不操作用户浏览器历史。

## 文件变更

- src/wallpaper-glass.css、src/theme.css、src/components/grouped-collection.css、wallpaper-settings-editor.tsx、src/App.tsx、types.ts、data/defaults.ts、lib/storage.ts：材质分层、参数与迁移。
- src/components/browser-history-view.tsx、src/hooks/use-browser-history-data.ts 及相关测试：数据容器生命周期、首读状态与失败提示。
- e2e/glass.spec.ts、e2e/history.spec.ts、版本期望测试：分层样式、保存取消和逐帧刷新回归。
- scripts/capture-materials.mjs、scripts/verify-native-history.mjs：生产截图和原生历史工作区切换帧验收；public/manifest.json 递增至 1.1.89。
- 更新外观、存储、工程维护、扩展交付、测试基线与当前状态，本日志双重登记。

## 测试与验证

- 类型检查通过；最终单元 42 文件、327 项通过。完整 E2E：162 passed / 40 skipped / 0 failed；末次边界补充后历史与玻璃专项 12 passed / 2 skipped。
- 初次单元有 5 项失败：旧版本字面量、权限检查后定时器时序、删除错误被刷新清除；修正实现与版本断言后通过。初次 E2E 有 3 项失败：页脚文字阴影（两个 project）与 Chromium alpha 舍入预期；去除页脚文字阴影并校准明确颜色值后完整通过，未跳过失败用例或放宽行为断言。
- 网页/扩展生产构建通过。26 张材质截图，浅深主题与页脚/菜单/管理器巡检；脚本零页面异常。深色配浅壁纸且零遮罩仍会低对比，保留压力图与明暗遮罩调节后的图，不强改用户设置。
- 原生 Chromium 151.0.7922.34：真实拒绝/允许/撤销/重授、125 条记录搜索/删除/双页面同步/刷新/重启通过；3 次切换共 50 帧，无空态或加载骨架闪动。
- 构建、解压交付目录和 ZIP 的 23 个文件逐文件 SHA256 一致；Wiki lint 要求 0 错误后提交。

## 问题、结果与下一步

- 根因一：分组局部样式在公共材质之后覆盖背景/滤镜，且引用不存在的边框变量；现已去除。
- 根因二：小控件共用重投影，分类滚动容器裁切叠加阴影；弹窗叠加实色内部容器和多层填色，卡片按钮重复玻璃底板。现按层级集中材质与投影，清理多余覆盖。
- 根因三：历史 loading 初始为 false 且延迟查询前没有加载态，视图切换会卸载数据状态；现保留数据容器并明确首读/刷新边界，隐藏新授权同样有加载保护。
- 交付 1.1.89，按协作契约提交并推送当前 main 分支；不创建发布标签。安装后到扩展管理页点击“重新加载”。构建大 chunk 告警仍保留，未扩大为依赖或全仓重构。
