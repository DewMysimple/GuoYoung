# 第012次

- 时间：2026-09-16 23:47:14（北京时间）
- 对话类型：工程维护
- 主题标签：GitHub、Release、Actions、扩展打包、1.1.76
- 源自原轮次：无
- 本轮目标：基于当前 1.1.76 构建扩展并发布 GitHub Release，确认下载资产可用。
- 日志索引：[[Wiki/日志/MOC_工作日志|工作日志 MOC]]
- 分类索引：[[Wiki/日志/工程维护/MOC_工程维护|工程维护 MOC]]
- 相关知识：[[Wiki/扩展与构建交付|扩展与构建交付]]、[[Wiki/当前状态|当前状态]]

## 已确认的决策

- 按当前源码与 `public/manifest.json` 的版本 `1.1.76` 发版，不递增到新的产品版本。
- 沿用现有 GitHub Actions 发布流程：推送 `v1.1.76` 标签，由 Windows runner 构建并上传 `site-hub-extension.zip`。

## 检查与操作

- 核对工作区、远程仓库、manifest 版本、发布工作流和扩展打包脚本；工作区在发版前无未提交源码改动。
- 完成本地类型检查、单元/组件测试、单 worker 全量 E2E、网页与扩展生产构建及扩展 ZIP 打包。
- 核对源码、扩展构建目录、固定解压目录和 ZIP 内的 manifest 版本均为 `1.1.76`。
- 推送 `v1.1.76` 标签，等待并核对 GitHub Actions 发布任务及 Release 资产状态。

## 文件变更

- 新增：`Wiki/日志/工程维护/第012次.md`。
- 修改：`Wiki/当前状态.md`、`Wiki/日志/MOC_工作日志.md`、`Wiki/日志/工程维护/MOC_工程维护.md`。
- 本轮未修改源码、manifest、构建脚本或依赖；构建目录、ZIP 和版本截图均按 artifacts 约定保留在忽略路径。

## 测试与验证

- `npm run typecheck`：通过。
- `npm test`：通过。
- `npm run test:e2e -- --workers=1`：`110 passed / 36 skipped / 0 failed`。
- `npm run build` 与 `npm run package:extension`：通过；ZIP 内 manifest 为 `1.1.76`。
- 真实 Chrome 截图巡检：桌面、移动端、管理分组、设置、新建分组、添加网站共 6 个场景通过，无横向溢出或未捕获页面异常；外部 favicon 的跨源阻拦仅为诊断日志，回退显示正常。
- `npm run lint:wiki`：通过，知识页 33 个、日志 116 篇、链接 1429 条。
- GitHub Actions 发布任务以 success 完成；Release `v1.1.76` 已发布，`site-hub-extension.zip` 资产状态为 uploaded。

## 问题、结果与下一步

- 排查期间一次并发 E2E 运行共用 `4173` 测试服务器，造成中途连接拒绝；清理残留进程后单独重跑完整基线通过，未产生产品代码变更。
- GitHub Release 已完成，可从 [v1.1.76 Release](https://github.com/DewMysimple/GuoYoung/releases/tag/v1.1.76) 下载 ZIP。安装更新后仍需到 `chrome://extensions` 或 `edge://extensions` 点击“重新加载”。
- 下一步沿用维护清单，继续关注 Vitest 大版本升级和窄窗口设置导航布局待办。
