# 第003次

- 时间：2026-08-16 11:48:05（北京时间）
- 对话类型：工程维护
- 主题标签：artifacts、目录整理、验收产物、输出路径、完整性
- 源自原轮次：无
- 本轮目标：按版本整理 artifacts 验收资料并让后续截图自动进入新目录，同时保持扩展交付路径不变
- 日志索引：[[Wiki/日志/MOC_工作日志|工作日志 MOC]]
- 分类索引：[[Wiki/日志/工程维护/MOC_工程维护|工程维护 MOC]]
- 相关知识：[[Wiki/扩展与构建交付|扩展与构建交付]]；[[Wiki/测试与验证基线|测试与验证基线]]

## 已确认的决策

- `artifacts/site-hub-extension/` 与 `artifacts/site-hub-extension.zip` 继续固定在根目录，不移动、不重命名且本轮不重新生成。
- 正式截图与录屏按扩展版本归入 `releases/v<version>/screenshots|recordings/`，早期无版本截图归入 `legacy/`，原始录屏和日志归入 `working/`。
- 所有旧素材只移动不删除；历史日志保持封存，不回写其中旧产物路径。
- 截图脚本和 E2E 统一读取 `public/manifest.json`，后续自动写入当前版本目录。

## 检查与操作

- 移动前记录受保护扩展目录的文件数与总字节数，并计算 ZIP 的 SHA-256。
- 对 70 个移动条目执行绝对路径、目标范围与目标冲突干跑校验，再在 `artifacts` 内完成分类移动。
- 新增目录说明并调整 `.gitignore`，使说明文件可保留而其余本地产物继续忽略。
- 检查活跃代码与说明中的产物路径，确认扩展 ZIP 仍是唯一固定写入根目录的文件产物。

## 文件变更

- 新增：`artifacts/README.md`、`artifacts/releases/`、`artifacts/legacy/`、`artifacts/working/` 与本日志。
- 修改：`scripts/capture.mjs`、`e2e/site-hub.spec.ts`、`.gitignore`、`Wiki/扩展与构建交付.md`、`Wiki/当前状态.md`、`Wiki/MOC_项目知识.md` 以及两份日志 MOC。
- 移动：51 张既有 PNG、29 个既有 WEBM 与 2 个服务器日志；无删除。

## 测试与验证

- `npx tsc -b --pretty false`：通过。
- `npm test`：15 个测试文件、111 例全部通过。
- `npm run test:e2e`：86 次执行中 62 passed、24 skipped；新截图成功写入 `artifacts/releases/v1.1.15/screenshots/`。
- `npm run build`：通过，仅保留既有大 chunk 警告。
- 真实浏览器截图抽检：新路径下的 Chromium 弹窗截图可正常读取。
- 保护复核：扩展目录仍为 67 个文件、15,868,103 字节；ZIP 仍为 319,234 字节，SHA-256 为 `8DFF394D6425ECD44EB71310ACC3CD9FDC1134F50D42166D227EC289D662F60A`。
- `npm run lint:wiki`：通过。

## 问题、结果与下一步

- `artifacts` 根目录现只保留固定交付项、README 与 `releases`、`legacy`、`working` 三个分类目录。
- 未修改产品行为、扩展源码或 manifest，扩展版本保持 1.1.15。
- 后续新增正式录屏时应按 README 手动放入当前版本的 `recordings/`；截图脚本与 E2E 已自动处理版本目录。
