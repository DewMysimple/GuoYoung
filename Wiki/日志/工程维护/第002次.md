# 第002次

- 时间：2026-08-09 14:35:00（北京时间）
- 对话类型：工程维护
- 主题标签：文档、README、AGENTS、lint
- 源自原轮次：无
- 本轮目标：补写 README.md 与 AGENTS.md 项目门面并挂载 lint:wiki 校验
- 日志索引：[[Wiki/日志/MOC_工作日志|工作日志 MOC]]
- 分类索引：[[Wiki/日志/工程维护/MOC_工程维护|工程维护 MOC]]
- 相关知识：[[Wiki/工作流与记忆维护|工作流与记忆维护]]

## 已确认的决策

- `README.md`（面向人）与 `AGENTS.md`（面向 AI）作为项目根门面，与 `Wiki/` 互补：README 介绍工程，AGENTS 规定每轮工作契约与硬约束。
- `AGENTS.md` 必须点名 6 条本工程专属硬约束，防止新 Agent 踩坑（npm.cmd、framer-motion 锁版、存储键不可换、拖拽改动先读契约、两段式协作、扩展改动出 ZIP 递增版本）。
- `lint:wiki` 挂入 `package.json` scripts，并写入 AGENTS 每轮流水线最后一步。
- `.gitignore` 补 `artifacts/` 与 `.codex-video-review/`（扩展打包产物、拖拽录屏诊断产物）。

## 检查与操作

- 复核 `package.json` 现有 scripts（dev/build/build:extension/package:extension/preview/test/test:watch/test:e2e），新增 `"lint:wiki": "node scripts/wiki-lint.mjs"`。
- 复核 `.gitignore` 现有 7 行，追加 `artifacts/` 与 `.codex-video-review/`。
- 编写 `scripts/wiki-lint.mjs`：移植 ChatAgent `wiki_lint.py` 的 19 条规则，新增 4 条（六分类、源轮次覆盖率审计、日志禁用 frontmatter、时间戳唯一），共 23 条校验。
- 编写 `README.md`：项目简介、两种形态对比、技术栈、快速开始、常用命令表、端口说明（5173 vs 4173）、目录导览、扩展安装、数据与隐私、测试、已知限制、工程记忆入口。
- 编写 `AGENTS.md`：开场声明（三层）、每轮启动 4 步、六分类判定、日志规范、双重登记、Wiki 沉淀边界、ADR 规则、6 条专属硬约束、每轮固定流水线、隐私边界。
- 校验 AGENTS 提到的每条命令真实存在于 `package.json`，每个 Wiki 路径真实存在。

## 文件变更

- 修改 `package.json`（新增 `lint:wiki` script）。
- 修改 `.gitignore`（追加 `artifacts/`、`.codex-video-review/`）。
- 新增 `scripts/wiki-lint.mjs`。
- 新增 `README.md`、`AGENTS.md`（项目根）。

## 测试与验证

- `node scripts/wiki-lint.mjs` 可正常执行；在 Wiki 未齐备时大量报错（缺文件/断链），证明校验器在工作而非崩溃。
- 修正脚本导入缺失（`fileURLToPath`）后稳定执行。
- `package.json` 与 `.gitignore` 改动经人工核对，未破坏既有脚本。

## 问题、结果与下一步

- 结果：项目门面与校验器到位，新会话只读 `AGENTS.md` + `Wiki/MOC_项目知识.md` 即可接手。
- 遗留：需在终验阶段实跑 `npm test` 与 `npm run test:e2e`，用真实测试数字回填「测试与验证基线」的引用值（当前为约数 89 / 40）。
- 下一步：由 [[Wiki/日志/测试验证/第001次|测试验证·第001次]] 完成终验与校准。
