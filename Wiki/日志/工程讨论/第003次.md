# 第003次

- 时间：2026-08-09 13:25:00（北京时间）
- 对话类型：工程讨论
- 主题标签：工程记忆、Wiki、方案、六分类
- 源自原轮次：无
- 本轮目标：确认工程记忆 Wiki 的落地位置、日志颗粒度、体系完整度与项目门面四项决策
- 日志索引：[[Wiki/日志/MOC_工作日志|工作日志 MOC]]
- 分类索引：[[Wiki/日志/工程讨论/MOC_工程讨论|工程讨论 MOC]]
- 相关知识：[[Wiki/工作流与记忆维护|工作流与记忆维护]]

## 已确认的决策

- Wiki 落地在 **WebPage 项目根内**（`WebPage/Wiki/`），作为工程记忆唯一真源；不另建独立仓库，避免与代码脱节。
- 日志按**工作单元合并**颗粒度：讨论轮 + 实施轮合一、同一 bug 多轮合一，而非 45 轮逐轮一篇。
- 采用**完整版**体系：六分类日志 + 双 MOC（总 MOC / 分类 MOC）+ 知识层主题页 + ADR + `lint:wiki` 校验脚本。
- 同时补写 **README.md 与 AGENTS.md** 作为项目门面。
- 接受两组额外合并：`008+009`（排序+拖拽整卡）、`019+020+021`（高清图标体系）。
- 原轮次 **030、040** 为纯诊断、知识密度高，独立成「工程讨论」日志（而非并入实施轮）。
- 允许改动 `package.json`（挂 `lint:wiki`）与 `.gitignore`（补 `artifacts/`、`.codex-video-review/`）。
- 否决方案：逐轮 1:1 转录（噪声大、不可维护）；仅建轻量日志不建知识层（失去跨会话架构沉淀）；Wiki 放独立仓库（易与代码失同步）。

## 检查与操作

- 通读桌面「工程记忆构建」方法论（`AGENTS.md` + `日志/README.md`）与 `ChatAgent/Wiki/` 成熟样板，确认三层架构（Raw sources / Wiki 事件层+知识层 / 规则层）。
- 读取 `MOC_019fe1f6...md` 总 MOC 与 45 轮对话素材，逐轮预分类并设计「45 轮 → 33 篇工作单元」映射。
- 核对 WebPage 真实工程事实：非 git 仓库、三处版本号不一致（`package.json 1.0.0` / `public/manifest.json 1.1.5` / `dist/manifest.json 1.1.0`）、无 README/AGENTS、端口 5173 与 4173 双套。
- 设计时间字段重建口径：原 001-041 轮 `created` 被统一写成 `2026-08-08 23:20:36`，按 `round_index` 正序每分钟 +1 回填并内联标注「重建值」，042-045 保留真实时间。
- 通过两轮 AskUserQuestion 与用户逐项确认上述决策，全部采用推荐项。

## 文件变更

- 本轮仅方案确认，无代码或文件改动。
- 落地工作由 [[Wiki/日志/工程维护/第001次|工程维护·第001次]] 与 [[Wiki/日志/工程维护/第002次|工程维护·第002次]] 执行。

## 测试与验证

- 无（纯方案讨论）。决策正确性以用户两轮确认回执为证。

## 问题、结果与下一步

- 结果：四项核心决策（位置 / 颗粒度 / 完整度 / 门面）与两项合并、两项独立、两项改动范围均已拍板。
- 遗留：45 轮合并映射是否完全覆盖 1-45、是否存在重复/越界，需在实施后用 lint 规则 21 审计。
- 下一步：按 A（骨架）→ B（lint 先行）→ C（33 篇日志）→ D（MOC 与知识层）→ E（README/AGENTS）→ F（终验）六阶段执行。
