# 第010次

时间：2026-08-08 23:10:36（重建值·源自原轮次 025）
对话类型：功能添加
主题标签：字号、作用域、外观
源自原轮次：025
本轮目标：将字号作用域扩张至 Logo 与卡片相对比例
日志索引：[[Wiki/日志/MOC_工作日志|工作日志 MOC]]
分类索引：[[Wiki/日志/功能添加/MOC_功能添加|功能添加 MOC]]
相关知识：[[Wiki/外观与设置体系|外观与设置体系]]

## 已确认的决策

- 把用户提出的"增加字号调节颗粒度"重新定义为作用域问题而非精度问题。检查后确认滑杆已经是 1% 步进，数值精度足够；真正缺的是截图中两个文字层级根本没有参与缩放。被否决的方案是继续细分滑杆步进（例如改成 0.5% 或 0.25%），理由是那样只会让数字更细，用户看到的两处文字依然纹丝不动。
- 本轮补进字号作用域的两个层级确定为：顶部品牌名，以及"全部网站 / 收藏数量"标题组。这两处正好对应用户要求的"增加 2 个颗粒度"。
- 卡片文字保持现有行为不变，本轮不改动其缩放规则，避免连带影响已经调好的卡片布局。
- 视觉上不做任何风格改动，只提升字号调节的覆盖面；预设、实时预览与数据校验链路保持原样。
- 本轮按用户明确要求重新构建并覆盖扩展 ZIP——这是自扩展落地以来连续多轮"不生成 ZIP"之后的第一次重新打包。

## 检查与操作

- 检查字号滑杆现状：确认已是 1% 步进，数值精度不是瓶颈，据此推翻"细分步进"的直觉方案。
- 对照用户提供的两张截图，逐一定位未参与缩放的文字层级，锁定顶部品牌名与列表标题组两处。
- 核对现有布局预设，确认新增两处接入后不会与紧凑/标准/宽松三档预设产生冲突。
- 核对实时预览链路，确认两处新接入的文字能与既有草稿预览机制共用同一条通路。
- 核对数据校验逻辑，确认字号数值仍在既有安全范围内被夹取，旧设置读取后视觉不变。
- 把顶部品牌名接入统一字号缩放，同时保证其旁边的图标尺寸不被文字比例带动。
- 把"全部网站 / 收藏数量"标题组接入同一字号缩放，保持标题与数量的相对层级关系。
- 验证桌面端与移动端两套布局在字号拉到区间两端时都不出现截断或换行异常。
- 并行运行组件测试与浏览器端回归，作为改动后的正式验收。
- 通过后执行扩展构建，覆盖生成最新 ZIP。
- 核对压缩包根目录结构与文件哈希，确认产物完整可分发。

```
artifacts/site-hub-extension.zip
SHA-256: 8F6D4F8A2688EB45686F3B6BE5BF128DCEA1ADA09BD92620A48823121CF72696
```

## 文件变更

修改：

```
src/App.tsx
src/styles.css
```

同批修改还包括顶部品牌区与站点列表标题区所在的 src/components/ 下相关组件，以及承载字号 CSS 自定义属性下发的样式变量定义。

新增：无

删除：无

产物更新：

```
dist-extension/
artifacts/site-hub-extension.zip
```

## 测试与验证

- 61 项单元/组件测试全部通过，与改动前基线持平，说明字号作用域扩张未影响既有逻辑。
- 浏览器端 28 项测试通过，4 项按设备条件跳过，无失败。
- 扩展构建执行成功并覆盖生成最新 ZIP。
- 产物核对：压缩包根目录结构正确，SHA-256 已记录，可用于分发与后续比对。
- 桌面端与移动端均已适配，字号调节保留 1% 精细步进与实时预览。

## 问题、结果与下一步

- 结果：字号调节新增两个生效层级（顶部品牌名、站点列表标题与收藏数量），补上用户要求的"2 个颗粒度"；原有 1% 精细调节与实时预览保留；本轮同时刷新了扩展产物，让 ZIP 重新与源码对齐。
- 遗留问题一：字号仍是"一个总比例带动所有层级"，用户无法把品牌名和卡片文字分开调，若后续再提相同诉求需要拆分数据结构。
- 遗留问题二：品牌名本身仍是硬编码文案，无法自定义。
- 遗留问题三：上一轮遗留的壁纸拖动流畅度与图标跨标签页缓存问题本轮未触及。
- 下一步：处理扩展环境下图标反复网络探测的问题，并把字号拆成多个独立可调层级。
