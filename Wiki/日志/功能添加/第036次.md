# 第036次

- 时间：2026-09-19 17:43:01（北京时间）
- 对话类型：功能添加
- 主题标签：可维护性、全工程体检、状态生命周期、回归测试、1.1.78
- 源自原轮次：无
- 本轮目标：逐一检查各界面与功能的维护风险，修复可复现边界并清理已确认冗余，完成验证和扩展交付
- 日志索引：[[Wiki/日志/MOC_工作日志|工作日志 MOC]]
- 分类索引：[[Wiki/日志/功能添加/MOC_功能添加|功能添加 MOC]]
- 相关知识：[[Wiki/全工程可维护性体检|全工程可维护性体检]]、[[Wiki/工程维护清单|工程维护清单]]、[[Wiki/数据与存储契约|数据与存储契约]]、[[Wiki/测试与验证基线|测试与验证基线]]

## 已确认的决策

- 按实际界面、业务边界和失败路径检查全工程；只清理有引用证据的冗余，以局部职责和状态同步点减少为重构收益。
- 分组排序使用单次会话，图片导入使用独立操作代次；保留网站 50px、分组 8px、18px 滞回、450ms 悬停、冻结槽位和 dnd-kit 唯一 transform。
- 收藏命令统一读取最新快照，保存失败可重试；GitHub 普通默认组只在初建补齐，之后删除不重建；存储键与 state v15 保持兼容。
- E2E 共用 fixture 并按业务域拆分，原测试体和设备矩阵保留；不通过放宽断言或增加跳过掩盖缺陷。
- 以 ADR-014 对齐源码实际图标候选/缓存行为，ADR-004 仅改替代元数据和关联链接，历史正文保留。本轮扩展版本 1.1.78，交付代码分支与 ZIP，不创建发布标签。

## 检查与操作

- 回读 AGENTS、MOC、状态及相关知识页，核对 src、e2e、配置、脚本与构建入口；矩阵覆盖主收藏、各类分组交互、GitHub、历史、设置、导入/回收站、图标、popup、书签和后台适配。
- 修改前类型和 223 例单元通过；TypeScript 引用/重复片段及运行时 import 图辅助定位，未发现运行时静态循环。CSS 结合类名引用和动态拼接人工确认，保留有效规则优先级。
- 根因与修复：同批操作混用旧 ref/React 队列导致修改覆盖；全局同步布尔开关误跳过随后本地保存；搜索与 effect 重复保存同一快照。现共用同步状态入口，按快照跳过和去重并显示保存失败。
- 根因与修复：GitHub 读取要求四个组而界面允许删除；进入工作区又重新补普通组。现只保护“其他”，通过 v11–v15 与实际删除/刷新回归。
- 根因与修复：异步图片缺少任务归属、卸载后创建 URL、失败路径未释放 IDB/位图；新增会话失效与 finally 释放。外部数据更新不再重置网站和仓库勾选草稿；历史部分失败保留可重试选择。
- 根因与修复：popup 先更新 UI 再保存，且可选 API 失败拖垮全部加载；改为保存成功后更新、失败隔离与重新读取，并在写入前读取已有共享修改。
- 补充探针确认 v15 缺失显示/排列字段时返回 recovered=false 但状态无效；五个版本回归首次失败。读取出口统一规范化偏好，保留收藏并保持幂等。
- 布局根因：分组控件使用视口外负坐标；设置断点只依赖窗口、不看剩余主区。预留控件空间并按顶栏容器宽度切换，补充边界与截图断言。

## 文件变更

- 新增：src/hooks/use-group-sort-session.ts、src/hooks/use-image-import.ts、src/components/dialog-navigation.tsx；七个就近测试文件，覆盖会话、图片、壁纸、持久化、编辑草稿与 GitHub 预览。
- 修改：src/App.tsx、src/hooks/use-site-hub.ts、src/hooks/use-wallpaper.ts，以及设置、编辑/分组/GitHub 导入、历史组件与 popup。
- 修改：src/lib/storage.ts、state-store.ts、site-state.ts、github-workspace.ts、github-repository-api.ts、wallpaper-store.ts、brand-logo.ts、bookmark-manager.ts、browser-favicon-placeholder.ts、group-sort.ts 和相关测试；清理无调用包装与重复函数。
- 删除/替换：e2e/site-hub.spec.ts 拆为九个业务域测试文件及 fixtures.ts；src/styles.css 删除失效规则并修正两处布局。
- 修改：public/manifest.json 至 1.1.78；新增全工程体检、ADR-014 和本篇日志，更新相关知识页、ADR-004 替代关系、MOC 与双重日志登记。
- 本地产物：artifacts/site-hub-extension、ZIP、v1.1.78 截图和 working 诊断资料；均不提交，未清理历史验收资料。

## 测试与验证

- npm.cmd run typecheck：通过；npm.cmd test：30 文件、264 例通过，比起点增加 41 例。
- 新增缺陷回归先失败再通过；整体验证中旧作者夹具错误继承 isProtected=true，修正为普通作者组。偏好规范化初稿将缺失 githubMigration 补成 null，导致两条导出测试失败；按可选字段与导出边界修改生产代码，保留原测试断言，最终全绿。
- npm.cmd run test:e2e -- --workers=1：最终 119 passed / 37 skipped / 0 failed；原 74 个测试体经 AST 对照一致，新增 4 个逻辑场景；独立服务器、单 worker，未新增设备跳过。
- 生产 popup 检查首次因测试视口 420px 小于 CSS 固定 440px 而报溢出；按实际扩展窗口 440×600 修正新夹具，增加页面宽度断言，定向两 project 共 2 例及生产 popup 巡检通过；未改产品尺寸或放宽断言。
- 网页与扩展生产构建、package:extension：通过；固定目录与 ZIP 根 manifest 均为 1.1.78。
- ZIP 含 22 个条目，373,425 字节；网页 JS 主包 884.22 kB（gzip 262.40 kB），扩展 main.js 629.15 kB。
- npm.cmd run capture、专项分组和生产数据/popup 巡检：通过；真实 Chrome 桌面/移动与各弹窗截图，900/1000/1100px 设置头部、分组选择、拖动中/结束后、GitHub 和历史截图复核。页面未捕获异常为 0，已检页面无横向溢出。
- 外部图标跨源错误单列诊断，不与应用异常混淆；大 chunk 提示保留。官方 registry 审计仍有两项 moderate 开发依赖记录，未批量升级依赖或修改 Motion 锁版。
- npm.cmd run lint:wiki：首轮指出新增体检主题缺少“依据”标题，补齐后 0 错误（35 个知识页、118 篇日志、1,475 条链接）；git diff --check：通过。

## 问题、结果与下一步

- 全工程检查矩阵、确认问题修复、冗余清理和验证交付完成；App 剩余网站拖拽 DOM/rAF、全局有效样式及大型设置/历史视图继续按收益渐进处理。
- 本次不宣称穷尽所有 Bug；真实扩展权限与原生跨页同步和模拟 API 区分，写前重读不是跨上下文原子事务；图片历史资源垃圾回收未实施。
- 交付 ZIP 位于 artifacts/site-hub-extension.zip；更新后到扩展管理页点“重新加载”。代码按当前 main 分支提交并推送 origin，发布标签保持原状。
