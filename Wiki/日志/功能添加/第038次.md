# 第038次

- 时间：2026-09-20 21:05:33（北京时间）
- 对话类型：功能添加
- 主题标签：设置优化、公共模块、浏览历史、壁纸协调、1.1.80
- 源自原轮次：无
- 本轮目标：落实文档中的尺寸调节、设置排版与公共模块要求，并按反馈修正壁纸细节。
- 日志索引：[[Wiki/日志/MOC_工作日志|工作日志 MOC]]
- 分类索引：[[Wiki/日志/功能添加/MOC_功能添加|功能添加 MOC]]
- 相关知识：[[Wiki/外观与设置体系|外观与设置体系]]、[[Wiki/系统架构|系统架构]]、[[Wiki/拖拽交互契约|拖拽交互契约]]、[[Wiki/决策/ADR-016|ADR-016]]、[[Wiki/测试与验证基线|测试与验证基线]]

## 已确认的决策

- 微调补齐卡片高度，新增恢复默认与本次最近预设；顶栏提供参数与边界拖动，侧栏去除左边阴影并共用尺寸手柄。设置草稿仍需保存，侧栏宽度保留原本自动记忆方式。
- 壁纸与数据页分卡组织，壁纸下保留原页面外形。用户否决整条白色底板；最终仅改善控件轻微透明与文字清晰度，管理入口收为完整圆角按钮、页脚提示去除文字阴影并使用贴合内容的小底色。
- 收藏/GitHub/历史共用搜索、卡片内容、打开与选择组件；历史权限/读取/刷新/删除归入 hook，搜索范围继续独立。业务状态与拖拽落点不合并为新全局状态源。
- 沿用 state v15、site-hub:v1 与既有字段，没有数据迁移或依赖升级；Motion 锁版保持。新增 ADR-016 替代 ADR-015，旧已接受正文保留。

## 检查与操作

- 阅读任务文档及图像参考，核对侧栏/顶栏、壁纸手势、卡片/搜索重复结构、历史生命周期与已有回归；只读参考工程的尺寸手柄实现。
- 提取受控壁纸/数据编辑器、统一 RangeControl/ResizeHandle、卡片基础组件与 WorkspaceSearch；移除旧重复 JSX、历史选择样式、废弃材质与尺寸拖动规则。
- settings-panel 从 1,275 降至 661 行，browser-history-view 从 1,189 降至 570 行；历史异步查询防止旧结果覆盖新结果，删除防重入，失焦取消活动拖动而不重建闲置搜索视图。
- 用原图本地导入验证壁纸读取与设置保存，复核桌面及窄屏；完成双构建、ZIP 内容校验和 Wiki 登记。

## 文件变更

- 新增：src/components 下的 card-primitives、workspace-search、range-control、resize-handle（含 CSS）、topbar-resize-handle、wallpaper-settings-editor、data-settings-editor、settings-editors.css、history-cards、history-card-content、history-range-control；src/hooks/use-browser-history-data.ts 及测试、use-settings-panel-width.ts；e2e/settings-refinement.spec.ts。
- 修改：src/App.tsx、src/styles.css、settings-panel、appearance-settings、site-card、browser-history-view、lib/appearance-settings 及对应单元/E2E，public/manifest.json。
- 文档：新增 ADR-016 与本日志，更新外观/架构/数据/拖拽/维护/交付/测试/状态及 MOC；旧 ADR 仅维护替代元数据和关联链接。
- 产物：artifacts/site-hub-extension.zip、固定解压目录与 artifacts/releases/v1.1.80/screenshots；构建、截图、原图与临时脚本不提交。

## 测试与验证

- 类型检查通过；最终完整单元/组件 **32 文件、271 passed**。
- 初次单元 **265 passed / 3 failed**：布局滑杆数量/作用域、历史部分删除后的刷新时序、选择态控件语义。收窄计数到布局微调，撤销提前移除历史项的生产变更，选择态按按钮查询；保留原业务断言，最终完整通过。
- 定向 E2E **23 passed / 5 skipped**；首次完整 **126 passed / 38 skipped / 2 failed**：壁纸页脚隐藏伪元素仍有旧背景计算值。清除旧分隔线背景、保留透明断言，最终完整 **128 passed / 38 skipped / 0 failed**，83 个逻辑场景、166 次项目执行。38 项为明确设备范围跳过，不算通过。
- 新回归覆盖尺寸拖动/取消/失焦/键盘/保存刷新、默认/上次预设跨设置页签、历史查询过期与删除重入、历史活动拖动失焦、壁纸无整条白底和页脚无文字阴影。
- 全量通过后的源码/截图复核发现历史详情排版规则误删，恢复有效规则、只保留选择控件去重，并补名称/网址分行对齐断言；修正后历史与设置定向 **11 passed / 1 skipped / 0 failed**。
- 网页与扩展生产构建通过；扩展 **1.1.80** ZIP 校验根 manifest 与主页面/popup/background 入口，共 **22 个条目、375517 字节**；大 chunk 告警保留。
- 常规 capture 与原图壁纸生产 Chrome 360/390/920/1440px 截图巡检通过，涵盖设置、平铺/分组、GitHub、历史总览/详情及页脚细节；无横向溢出，未捕获页面异常为 0。历史使用模拟 API，未声称原生授权流程全覆盖。
- 常规 capture 单列 1 次外部图标资源跨源阻止诊断，页面未捕获异常为 0；未把网络图标诊断记为应用崩溃。
- 提交前执行 Wiki 校验与 Git diff 检查；本轮源码和文档统一提交并推送 main，未创建发布标签。

## 问题、结果与下一步

- 设置和历史从集中视图拆为数据生命周期、受控编辑及共用交互，减少同样的样式/行为需要多处修改的问题；保留收藏分组既有排序与选择契约。
- 初版壁纸协调过度铺白、管理入口缺少完整按钮边界、页脚依靠阴影导致发虚，是本轮视觉返工的原因；最终按原外形和轻微透明感修正。
- App 的网站拖拽 DOM/rAF 仍集中，后续按维护清单处理；本轮未批量拆文件或升级依赖，也未宣称全部维护问题消失。
- 交付 1.1.80；更新后需到扩展管理页点击“重新加载”。
