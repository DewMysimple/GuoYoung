# 第039次

- 时间：2026-09-20 22:12:08（北京时间）
- 对话类型：功能添加
- 主题标签：分组显示、可维护性、公共模块、对齐、1.1.81
- 源自原轮次：无
- 本轮目标：治理收藏主页全部分组显示的代码耦合，优化分组图标和加号并统一对齐，交付经验证的 1.1.81。
- 日志索引：[[Wiki/日志/MOC_工作日志|工作日志 MOC]]
- 分类索引：[[Wiki/日志/功能添加/MOC_功能添加|功能添加 MOC]]
- 相关知识：[[Wiki/收藏分组模块边界|收藏分组模块边界]]、[[Wiki/拖拽交互契约|拖拽交互契约]]、[[Wiki/测试与验证基线|测试与验证基线]]

## 已确认的决策

- 保留页面原外形及壁纸轻透效果；分组列表取消 20px 整体右移及网格 2px 内距，加号移入标题操作区。加号/管理共用 28px 无阴影按钮，分组图标与首张卡片左对齐，多选与页面操作右对齐，全部控件垂直居中。
- 平铺/单组/分组共用网站网格，标题、插入菜单、sortable 外壳按职责分离；选择 reducer、持久化入口和单一意图提交继续沿用。
- 不以单纯拆文件作为可维护性完成标准：原生事件/rAF 使用最新回调，取消与边缘滚动由实际共享模块提供，拖拽遇真实数据更新取消旧事务。
- 保持 state v15、site-hub:v1、Motion 12.23.12、50px/8px/18px/450ms 契约，无依赖升级或新增状态库。

## 检查与操作

- 回读协作入口、状态、维护清单、拖拽契约及三张标注图片；核对分组标题、网格、App 拖拽与 E2E。
- 对齐根因：左侧预留缩进、网格额外 padding 与加号负偏移/固定高度叠加，导致页面基线及垂直中心分离。
- 生命周期风险：空依赖原生监听器捕获首次分组/选择状态；窗口取消缺少 pending/pagehide，几何延迟回捕缺少会话校验；分别用最新回调、公共取消和快照匹配修复。
- 静态相对 import 图检查 85 个运行时 TS/TSX 模块，无循环。App 从 3,528 行降至 2,181 行；sortable 外壳从 289 行降至 36 行，网站事务 682 行、分组排序编排 191 行；数字用于描述职责范围，不作为性能或零 Bug 保证。
- 使用构造收藏数据完成生产浏览器巡检；原图壁纸仅本地读取，截图和诊断资料留在 artifacts，不提交用户图片。

## 文件变更

- 新增：grouped-collection、group-section-header、group-insert-control、compact-icon-button、collection-site-grid 及分组 CSS；网站/分组拖拽、窗口事件、最新回调、标签滚动、防误点击 hooks；collection-drag 几何、标识与传感器模块。
- 修改：App、group-drop-target、sortable-group-section 和全局样式；manifest 递增 1.1.81。保持单一持久化与选择状态，不改变已有用户数据。
- 测试：新增 use-drag-window-events / use-site-dragging 回归和 grouped-layout E2E；更新旧分组加号外观断言及组件插入入口的键盘操作。
- 文档：新增收藏分组模块边界；更新架构、拖拽、外观、维护、体检、当前状态、验证基线与扩展交付；本日志双 MOC 登记。

## 测试与验证

- typecheck 通过；单元/组件 34 文件、278 passed；完整 E2E 单 worker：134 passed / 38 skipped / 0 failed，共 86 个逻辑场景、172 次执行，设备范围 skip 未增加。
- 中间失败完整记录：初次提取漏 Fragment 导入，导致 App 57 例失败，修复后再次运行；局部 CSS 进入 jsdom 后 1 例插入按钮 pointer 操作被隐藏样式阻止，改用真实可用的键盘激活，浏览器继续验证鼠标与触屏。
- 首次拖拽定向 39 passed / 24 skipped / 3 failed：两项仍断言旧外置圆形阴影，与用户新需求冲突，改为行内居中、8px 圆角与无阴影；一项触屏迁移被浏览器兼容 pointercancel 提前取消，改由 touchcancel 结束 TouchSensor。
- 次轮定向 7 passed / 1 skipped / 2 failed：pending React 状态尚未提交时传感器释放事件递归取消；增加重入保护与对应单元测试。最终相关定向 7 passed / 1 skipped，完整 E2E 随后全量复核通过。
- 追加边界复核确证分组在 8px 激活前失焦后继续移动仍出现浮层（实测数量 1）；补齐顶部标签/纵向标题 pending 注册和标签失焦长按取消。增强既有取消 E2E，包含 520ms 后不打开管理器、不产生浮层、不改分组顺序；定向桌面/移动 6 passed，随后重新执行完整流水线。
- 网页/扩展生产构建与 ZIP 通过，产物 manifest 1.1.81；大 chunk 提示保留。生产 Chrome 117 个构造网站、360/390/920/1440/1920px 普通/插入/选择/壁纸截图，桌面拖动开始/让位/取消逐帧检查；无页面异常、无横向溢出。截图复核后给壁纸下紧凑按钮增加局部透底和文字对比度，随后重新完整 typecheck、278 单元、134 passed / 38 skipped E2E、双构建和截图。常规 capture 完成，另记录 1 类外部图标跨域拦截网络错误，不计作页面异常。
- Wiki 校验与 Git diff 检查通过；所有本轮源码/知识变更本地提交，并推送当前 main 分支到 origin。不创建发布标签。

## 问题、结果与下一步

- 已完成标注位置对齐、加号/管理图标统一及分组显示公共模块治理。维护入口可从收藏分组模块边界表直接定位，不再为改标题或网格进入大型拖拽作用域。
- 网站意图分支仍较复杂，后续按具体失败证据维护；不宣称所有未来 Bug 消失。持久化跨页面写入仍非原子事务，测试浏览器 API 仍有模拟边界；本次不升级依赖、不处理包体积告警。
- 固定 ZIP 为 artifacts/site-hub-extension.zip，固定解压目录保留；用户更新后到扩展管理页点“重新加载”。
