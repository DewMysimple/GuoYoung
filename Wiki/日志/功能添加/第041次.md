# 第041次

- 时间：2026-09-23 11:03:07（北京时间）
- 对话类型：功能添加
- 主题标签：明暗主题、系统跟随、整体适配、增量迁移、1.1.83
- 源自原轮次：无
- 本轮目标：在设置加入跟随系统、浅色、深色，并适配整体 UI 风格。
- 日志索引：[[Wiki/日志/MOC_工作日志|工作日志 MOC]]
- 分类索引：[[Wiki/日志/功能添加/MOC_功能添加|功能添加 MOC]]
- 相关知识：[[Wiki/外观与设置体系|外观与设置体系]]、[[Wiki/数据与存储契约|数据与存储契约]]、[[Wiki/测试与验证基线|测试与验证基线]]

## 已确认的决策

- 外观页首部增加主题三段按钮，沿用原有卡片、图标与圆角；选择即预览，保存才保留，取消恢复。新安装默认跟随系统，旧固定浅色安装升级保持浅色。
- appearance.theme 进入统一草稿与 state v16；site-hub:v1 保持，迁移只补不覆盖，整库备份保留主题；不恢复历史根字段 themePreference。
- 共用 theme.css 与 useTheme，覆盖主页、分组、GitHub、历史、弹窗、壁纸和扩展 popup；不增依赖，不改 Motion 版本或拖拽算法。

## 检查与操作

- 按 AGENTS 读取 MOC、当前状态、外观/存储/交付/拖拽契约，并核对用户设置截图及固定浅色源码。
- useTheme 在 layout effect 内解析主题与强调色，只在 system 模式监听系统变更，切换/卸载清理；同步原生 color-scheme 与 theme-color。
- 明暗表面/边界/文字、警告/危险/成功提示统一变量；强调文字在深色提亮，危险实底与危险文字分离，选择勾保持白色。壁纸遮罩与文字阴影随主题切换，保持原形状与轻透。
- 第一轮生产截图发现 GitHub 等透明黑色图标与深色底对比不足，给已加载/内置品牌图标补小面积中性浅底，图像原色保持；补回归断言并重跑完整流水线。
- 修正知识页仍描述旧外置插入控件、固定浅色和当前 v15 的过时说明；历史日志与 accepted ADR 正文保持封存。

## 文件变更

- src/theme.css、hooks/use-theme、components/appearance-settings、App：主题令牌、系统订阅与受控设置；styles/grouped-collection/popup 适配。
- types/defaults/storage：appearance.theme 类型、默认值与 v16 迁移；public/manifest.json：1.1.83。
- hooks/use-theme.test、lib/theme-migration.test、App/storage/site-state 测试及 e2e/theme/settings/popup：主题、存储和交互回归。
- e2e/fixtures.ts：明确验证每个测试的新 context 存储为空，单次导航等待应用入口，移除初始化时多余的清空与重载；业务用例的刷新断言保持。
- Wiki 外观、存储、架构、维护清单、当前状态、验证基线、交付页、MOC 与本日志双重登记。

## 测试与验证

- 类型检查通过；单元/组件 40 文件、305 passed；完整 E2E 单 worker：144 passed / 38 skipped / 0 failed，91 逻辑场景 × 2 project。跳过为原设备范围，未计为通过。
- 初次单元 304 passed / 1 failed：回收站迁移旧夹具日期自然过期，改为当前删除时间后原断言通过。主题初次定向 4 failed：新测试误认侧栏实体底色与返回确认行为，核对既有 97% 透明底和返回直接取消语义后修正；第二次定向 4 passed，最终全量通过。
- 图标补色后全量 143 passed / 38 skipped / 1 failed：collection 分组显示场景在 fixture 第二次导航报 net::ERR_ABORTED，未进入业务断言。中断的浏览器底层触发原因未证实；初始化先检查独立 context 存储为空，单次导航等应用就绪，移除冗余重载，业务刷新与断言全部保留，随后完整重跑。
- 网页/扩展生产构建通过；真实 Chrome 两套主题在 360/390/920/1440px 检查主页、设置/数据、分组/选择、管理/确认、网站编辑、GitHub 与原图壁纸，桌面检查拖动开始/让位/取消；另有模拟历史与扩展生产 popup 双页签、常规 capture。
- 六个预设强调色的深色正文/次级/辅助/强调/危险文字与对应底色抽样对比度 >=4.5:1；未见横向溢出，未捕获页面异常为 0。截图归档 artifacts/releases/v1.1.83/screenshots。
- 对比度探针初次被全局 reduced-motion 的极短过渡干扰，连续两次读到同一帧颜色；探针禁用自身过渡并等待当前强调色实际应用后重测，不修改产品对比度断言。
- ZIP、固定解压目录与 dist-extension 逐文件 SHA256 匹配，manifest 均为 1.1.83；Wiki 校验作为提交前最后一道检查执行。构建大 chunk 告警保留。
- 常规 capture 记录一条远程图标资源 ERR_BLOCKED_BY_RESPONSE.NotSameOrigin；未捕获应用异常为 0，页面继续走既有图标回退，不把远程图标可用性当作主题回归结论。

## 问题、结果与下一步

- 三种主题及整体适配完成；旧收藏与自定义参数保留。交付后在扩展管理页点“重新加载”。
- 历史与 popup 的原生 API 使用测试替身，本轮不声称复验所有原生权限弹窗；主题本身使用真实 Chrome 系统颜色模拟与生产渲染验证。
- 本轮按契约提交并推送当前分支，未创建 Release 标签；后续主题维护集中于共用令牌与单一偏好，避免各组件独立配色漂移。
