---
type: status
status: active
created: 2026-08-09
updated: 2026-09-10
tags:
  - status
  - overview
---

# 当前状态

[[Wiki/MOC_项目知识|← 返回项目知识 MOC]]

> 接手摘要。交互细节见对应主题页，逐轮变化见日志；不在本页复制完整 UI 演进史。

## 产品与协作

- 对外名称：**Mysimple · 网站收藏**。同一 React 源码交付网页与 Chrome / Edge MV3 新标签页扩展，无账号与自建后端。
- Git 远程：`origin` → [DewMysimple/GuoYoung](https://github.com/DewMysimple/GuoYoung.git)。文件修改经验证后由 Agent 提交并推送当前分支；发布标签按具体任务执行。
- 用户明确要求实现、修复或维护，即在任务范围内执行；复杂任务说明安排并持续汇报，**不要求固定口令或再次确认计划**。ADR-011 已由 [[Wiki/决策/ADR-013|ADR-013]] 取代。
- 新会话先读 AGENTS、本页与 MOC，按任务读主题页；本期评估和优先级见 [[Wiki/工程维护清单|工程维护清单]]。

## 已实现能力

| 能力 | 当前范围与详细契约 |
| --- | --- |
| 收藏与分组 | 添加 / 编辑 / 搜索、分组管理、拖拽排序与迁移、链接和分组批量选择、Shift 范围选择；见 [[Wiki/拖拽交互契约\|拖拽交互契约]] |
| 排列与显示 | 按工作区持久化平铺 / 分组显示，以及手动、名称、时间和热度排列；热度卡片显示纯数字点击次数 |
| GitHub 工作区 | 顶栏“收藏主页 → GitHub → 历史记录”；GitHub URL 分流、跨工作区搜索、作者公开仓库预览导入、全来源只追加刷新与按作者显示结果；见 [[Wiki/扩展与构建交付\|扩展与构建交付]] |
| 浏览历史 | 可选 history 权限、按站点聚合与详情页、独立搜索和时间筛选、URL 级删除、多选、后退恢复滚动；历史卡片拖拽只有浮层反馈，不提供收藏落点或排序提交 |
| 外观设置 | 浅 / 深色、主题色、卡片几何、自定义品牌、壁纸；预览与保存分离，未保存草稿关闭前提示；见 [[Wiki/外观与设置体系\|外观与设置体系]] |
| 图标 | 七级回退、64px 高清门槛、占位图校验、来源选择与缓存；见 [[Wiki/图标解析体系\|图标解析体系]] |
| 导入与恢复 | 整库 JSON、单组资源包、回收站恢复和保留期限；见 [[Wiki/数据与存储契约\|数据与存储契约]] |
| 扩展弹窗 | 当前页收藏与浏览器书签管理 / 导入；复用扩展本地存储 |

## 兼容与交付基线

- 收藏 state **v14**；存储键恒为 `site-hub:v1`。网页用 localStorage，扩展用 chrome.storage.local，本地壁纸在 IndexedDB；迁移只补不覆盖。
- `public/manifest.json` 为扩展版本真源，当前 **1.1.73**；`package.json` 的 1.0.0 独立于产品扩展版本。本期只维护文档、测试和开发工具，不递增扩展版本。
- 最近已记录的 GitHub Release 为 `v1.1.69`；源码可能领先发布包，最新资产以 Releases 页面为准。
- 网页输出 `dist/`，扩展输出 `dist-extension/`；固定解压交付目录与 ZIP 位于 artifacts 根，截图按扩展版本归档，临时诊断在 artifacts/working。
- 普通提交 / PR 使用 Validate project 工作流验证；版本标签继续触发 Publish extension release 工作流，二者使用 Node 24。

## 当前验证

本期验证于 2026-09-10 完成；详细结果见 [[Wiki/测试与验证基线|测试与验证基线]] 和 [[Wiki/日志/工程维护/第011次|本期维护日志]]。

- 单元与组件：20 文件、196 例通过；独立服务器 E2E：110 passed / 36 skipped / 0 failed。
- 网页 / 扩展生产构建、7 场景生产 Chrome 巡检和 Wiki 校验完成；设置侧栏的窄窗口导航问题见下方待办。
- 本期发现旧 E2E 的两个分组排序用例在视口外发起鼠标动作；修正为先滚入视口，并增加激活与刷新后持久化校验。原先的“既有时序问题”判断已更正，不作为可接受失败基线。
- 实际 Chrome / Edge 扩展安装中的权限提示、原生事件同步仍需与网页模拟测试区分；不能以模拟 API 通过代替实机扩展验收。

## 正在跟踪

1. **开发依赖升级**：nanoid 已补丁更新；Vitest / @vitest/mocker 仍有同一安全公告关联的 2 项 moderate 审计记录，需专项大版本升级，详见维护清单。
2. **代码体积与维护成本**：App、全局 CSS、设置 / 历史组件及单个 E2E 文件较大，随相关功能逐步拆分；暂不整体重构。
3. **E2E 并发**：保留 fullyParallel 能力，完整验收与 CI 使用 1 worker，避免已知资源竞争；移动端部分测试为桌面专用场景而明确跳过。
4. **交互边界**：继续关注密集网格、偏心抓取、复杂缩放；拖拽活动中取证使用视口截图，避免全页截图干扰滚动与落点。
5. **浏览器限制**：原生新标签搜索框不可嵌入、无痕新标签不接管、工具栏固定需手动；Chrome / Edge 历史按配置文件独立，删除粒度为 URL 而不是单次 visit。
6. **窄窗口设置导航**：约 920px 窗口打开设置侧栏后，剩余主区域的顶部导航文字会挤成竖排；本期记录为 P2 布局待办，未改动产品样式。
7. **本地数据与网络边界**：无跨设备自动同步；图标、在线图片、搜索和 GitHub 导入需要网络。备份不包含回收站、搜索 / 浏览器历史及本地壁纸文件。

## 依据

- [[Wiki/日志/工程维护/第011次|工程维护·第011次]] — 本期源码核对、结构评估与验证记录。

## 相关页面

- [[Wiki/工程维护清单|工程维护清单]]
- [[Wiki/系统架构|系统架构]]
- [[Wiki/测试与验证基线|测试与验证基线]]
- [[Wiki/工作流与记忆维护|工作流与记忆维护]]
- [[Wiki/扩展与构建交付|扩展与构建交付]]
