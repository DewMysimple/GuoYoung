# AGENTS.md — WebPage（site-hub / Mysimple · 网站收藏）Agent 协作契约

> 本文件是 Agent 每轮工作的契约。新会话 / 新 Agent 接手时，**先读本文件**，再读 `Wiki/MOC_项目知识.md` 与 `Wiki/当前状态.md`，不要默认加载全部历史对话。

## 1. 记忆分层与事实优先级

- **事实层**：`src/` 源码与测试是唯一事实来源。
- **事件层**：`Wiki/日志/` 是不可改写的流水账（每轮一篇，封存不改写）。
- **知识层**：除日志以外的 `Wiki/`（主题页、ADR、MOC、状态页）是可修订的沉淀。
- **冲突以代码为准**：Wiki 与源码冲突时，以代码为准，并在当轮修正 Wiki（更新对应知识页，bump `updated`）。

## 2. 每轮启动 4 步

1. 读本文件（`AGENTS.md`）。
2. 读 `Wiki/MOC_项目知识.md` 与 `Wiki/当前状态.md`。
3. 按任务只读相关主题页 / ADR / 少量日志，**不默认加载全部历史**。
4. 用源码（`rg` / 直接读）核实关键事实；与 Wiki 冲突时以代码为准并当轮修正。

## 3. 六分类判定规则

每轮只有一个主要类型决定物理目录：
- **功能添加**：新功能、行为变化、重构。
- **UI修改**：纯界面 / 外观 / 样式调整。
- **Bug处理**：已知异常的诊断、恢复、修复。
- **工程讨论**：解释、比较、架构讨论、计划、待办。
- **测试验证**：只做测试、审查、检查、验收。
- **工程维护**：日志、wiki、文档、配置、依赖、清理、迁移。
混合任务按主要交付物分类；实现过程含测试仍归实现类。用户指定类型时优先采用。

## 4. 日志写作规范

- 模板见 `Wiki/日志/README.md`。日志文件**无 frontmatter**，首行 `# 第NNN次`。
- 五个固定二级标题：`## 已确认的决策` / `## 检查与操作` / `## 文件变更` / `## 测试与验证` / `## 问题、结果与下一步`。
- 元数据用**全角冒号**：`- 时间：`、`- 对话类型：`、`- 主题标签：`、`- 源自原轮次：`、`- 本轮目标：`、`- 日志索引：`、`- 分类索引：`、`- 相关知识：`。
- 时间：历史日志时间为重建值（内联标注「重建值·源自原轮次 X-Y」）；**此后新日志一律用真实北京时间，不再回填**（见 `Wiki/日志/README.md` 时间字段口径）。
- Bug 类日志必须写**根因**而非仅现象。

## 5. 双重登记

- 每篇日志必须在**总 MOC**（`Wiki/日志/MOC_工作日志.md`，跨分类时间倒序）与**唯一分类 MOC**（如 `Wiki/日志/功能添加/MOC_功能添加.md`，本类编号倒序）各登记一次。
- 表格内 wiki-link 别名分隔符必须写作 `\|`（反斜杠 + 竖线），否则 Obsidian 表格会错位。

## 6. Wiki 沉淀边界

只把**跨会话有效**的内容沉淀进知识层：架构 / 接口 / 约束 / 状态、已确认决策及其后果、反复踩坑。一次性问答、临时调试不进 Wiki。

## 7. ADR 规则

- 一篇一决定；标题即决定。
- 已接受（accepted）的 ADR **不改写正文**。
- 决策被推翻时：新增 ADR，旧 ADR 标 `status: superseded` + `superseded_by`，新 ADR 标 `supersedes`，两者在 `## 相关页面` 互相链接（例见 `Wiki/决策/ADR-005` 与 `ADR-006`）。

## 8. 本工程专属硬约束（防踩坑，最关键）

1. **Windows 上用 `npm.cmd`** 规避 PowerShell 对 `npm.ps1` 的执行策略拦截（在 Node 脚本里用 `npm.cmd`，shell 里直接 `npm` 即可）。
2. **排序位移只允许 dnd-kit 控制，禁止叠加 framer-motion 的 transform**；`framer-motion` **不得升级**（锁定 `12.23.12`，`package.json` 有 `overrides.motion-dom`）。
3. **存储键 `site-hub:v1` 不可更换**；新增 state 字段必须写迁移且**只补不覆盖**（见 `Wiki/数据与存储契约.md`）。
4. **拖拽任何改动必须先回读 `Wiki/拖拽交互契约.md`**——该区域已迭代十轮，坑极多（50px 阈值、冻结槽位、18px 滞回、rAF 交叠面积、跨组落点意图）。
5. **大改动走两段式**：先出 `proposed_plan`，等用户回 `PLEASE IMPLEMENT THIS PLAN` 再实施（自 037 轮起固化）。
6. **涉及扩展改动**：出 ZIP、递增 `public/manifest.json` 版本、附「到扩展管理页点重新加载」提示（见 `Wiki/扩展与构建交付.md`）。
7. **Git 协作**：当前远程仓库为 [`DewMysimple/GuoYoung`](https://github.com/DewMysimple/GuoYoung.git)，本地远程名固定为 `origin`；本轮只要产生文件修改，就必须在交付前创建本地 Git commit，并将当前分支推送到 `origin`。Agent 负责执行本地提交和远程分支推送；发布标签是否推送按具体发布任务执行。

## 9. 每轮固定流水线

```
类型检查 → npm test → npm run test:e2e → 生产构建 → 真实浏览器截图巡检 → npm run lint:wiki
```

- 类型检查通过是底线（`npm run build` 含 `tsc -b`）。
- 拖拽 / 布局 / 图标类改动**必须真实浏览器截图或录屏逐帧核对**，自动化无法完全覆盖。
- 提交前 `npm run lint:wiki` 必须 0 错误。
- 所有文件修改完成并通过必要校验后必须提交 Git，并推送当前分支到 `origin`；交付时不得留下未提交或未推送的本轮改动。

## 10. 隐私边界

只记录决策、命令目的、文件范围、测试摘要、错误类别、最终结果。不复制完整聊天、私人消息、内部推理、密钥或大段命令输出。

---
工程记忆导航：[[Wiki/MOC_项目知识|项目知识 MOC]] · [[Wiki/当前状态|当前状态]] · [[Wiki/日志/MOC_工作日志|工作日志 MOC]] · [[Wiki/决策/MOC_决策|决策 MOC]]
